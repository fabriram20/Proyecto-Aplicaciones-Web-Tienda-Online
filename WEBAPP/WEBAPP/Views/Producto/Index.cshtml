@model IEnumerable<WEBAPP.Producto>

@{
    ViewBag.Title = "Productos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!--Codigo autogenerado
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Foto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Precio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreProducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Foto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Precio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProducto }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProducto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProducto })
        </td>
    </tr>
}

</table>
-->


<div class="container">

    <div class="row">

        <!--Datos cliente - Total-->
        <div class="col-sm-2">
            <img src="../images/vertical2.jpg" class="img-responsive center-block"
                 alt="Responsive banner" style="max-width: 100%; height: auto; display: block;">
        </div>

        <!--Listado de Productos-->
        <div class="col-sm-8">
            <h1>Productos</h1>

            <!--Crear producto-->
            <!--
            <form class="animated fadeIn" [hidden]="!admin" (ngSubmit)="crearProducto(NuevoProductoForm)" #NuevoProductoForm="ngForm">
                <div class="form-group jumbotron">
                    <label>Nombre Producto</label>
                    <input type="text" placeholder="Ingrese nombre producto" class="form-control" name="nombreProducto" [(ngModel)]="nuevoProducto.nombreProducto">
                    <label> Foto </label>
                    <input type="text" placeholder="Ingrese foto" class="form-control" name="foto" [(ngModel)]="nuevoProducto.foto">
                    <label> Precio</label>
                    <input type="number" step="0.01" placeholder="Ingrese precio" class="form-control" name="precio" [(ngModel)]="nuevoProducto.precio">
                    <label>Categoria: </label>

                    <select class="form-control" name="idCategoria" [(ngModel)]="nuevoProducto.idCategoria">
                        <option *ngFor="let categoria of categorias" value='{{categoria.id}}'>
                            {{categoria.nombreCategoria}}
                        </option>
                    </select>
                </div>
                <button type="submit" class="btn btn-block btn-success">Crear Producto</button>
                <br>
            </form>
                -->

            <!--Listar Producto-->

            <table class="table table-responsive table-striped center" style="text-align:center;">
                
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Foto)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.NombreProducto)
                    </th>                    
                    <th>
                        @Html.DisplayNameFor(model => model.Precio)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <img src="../images/@Html.DisplayFor(modelItem => item.Foto)" style="height: 150px; width: 150px;" alt="Responsive image" class="img-thumbnail">                            
                        </td>
                        <td>
                            <h4>@Html.DisplayFor(modelItem => item.NombreProducto)</h4>
                        </td>                        
                        <td>
                            <h4>@Html.DisplayFor(modelItem => item.Precio)</h4>
                        </td>
                        <td>
                            <a class="btn btn-block btn-warning">Añadir al carrito</a>                            
                        </td>
                        <!--
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.IdProducto }) |
                            @Html.ActionLink("Details", "Details", new { id = item.IdProducto }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.IdProducto })
                        </td>
                            -->
                    </tr>
                }              
                
            </table>
        </div>

        <!--Ofertas y publciidad-->
        <div class="col-sm-2">
            <img src="../images/vertical.jpg" class="img-responsive center-block"
                 alt="Responsive banner" style="max-width: 100%; height: auto; display: block;">
        </div>

    </div>


</div>